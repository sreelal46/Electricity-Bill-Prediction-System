<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{title}}</title>

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">

  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>

  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
  </style>
</head>

<body class="min-h-screen flex items-center justify-center bg-gradient-to-b from-slate-100 to-white text-slate-800">

  <div class="w-full max-w-md px-4 text-center">

    <!-- Logo -->
    <div
      class="mx-auto mb-6 w-14 h-14 bg-teal-600 text-white rounded-xl flex items-center justify-center shadow-lg text-xl">
      ⚡
    </div>

    <h1 class="text-3xl font-bold mb-2">Sign in to your account</h1>
    <p class="text-sm text-slate-500 mb-8">
      Or <a href="/" class="text-teal-600 hover:underline">return to home page</a>
    </p>

    <!-- Card -->
    <div class="bg-white rounded-2xl shadow-xl p-8 text-left">

      <!-- Toggle -->
      <div class="bg-slate-100 p-1 rounded-lg flex mb-6">
        <div class="w-1/2 text-center py-2 rounded-md bg-white font-medium shadow">
          User Login
        </div>
        <a href="/admin/login" class="w-1/2 text-center py-2 rounded-md text-slate-500 hover:text-slate-700">
          Admin Login
        </a>
      </div>

      <!-- Form -->
      <form action="/user/login" method="POST">

        <label class="block text-sm font-medium mb-2">Email address</label>
        <input type="email" name="email"
          class="w-full mb-4 px-4 py-3 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-teal-500"
          placeholder="user@example.com" required />

        <label class="block text-sm font-medium mb-2">Password</label>
        <input type="password" name="password"
          class="w-full mb-4 px-4 py-3 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-teal-500"
          placeholder="••••••••" required />

        <div class="flex justify-between items-center mb-6 text-sm">
          <label class="flex items-center gap-2">
            <input type="checkbox" />
            Remember me
          </label>
          <a href="#" class="text-teal-600 hover:underline">Forgot password?</a>
        </div>

        <button class="w-full bg-slate-900 text-white py-3 rounded-lg hover:opacity-90 transition">
          Sign in →
        </button>

      </form>

      <div class="text-center mt-6 text-sm text-slate-500">
        <div class="flex items-center gap-3 mb-3">
          <div class="flex-1 h-px bg-slate-200"></div>
          New to EnergyGuard?
          <div class="flex-1 h-px bg-slate-200"></div>
        </div>

        <a href="/user/register" class="text-teal-600 font-medium hover:underline">
          Create an account
        </a>
      </div>

    </div>

  </div>

</body>

</html>